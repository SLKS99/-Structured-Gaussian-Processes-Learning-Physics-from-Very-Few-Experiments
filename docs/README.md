# Docs

- `sgp_tutorial.html` is the rendered version of the main notebook for easy reading/sharing.
- To re-generate it from the notebook, run `..\scripts\export_html.ps1` from the repo root (PowerShell).

